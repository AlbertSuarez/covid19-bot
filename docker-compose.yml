version: '3.5'
services:
  covid19-bot:
    image: covid19-bot
    container_name: covid19-bot
    env_file:
      - .env
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    volumes:
      - './data:/srv/covid19-bot/data'
      - './tor:/srv/covid19-bot/tor'
